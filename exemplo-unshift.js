let playlist = ['funk', 'hip-hop', 'pop']

console.log(`antes do unshift: ${playlist}`)

//adcionando uma nova música no inicio da playlist
playlist.unshift('rock')

console.log(`depois do unshift: ${playlist}`)
