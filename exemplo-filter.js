let playlist = ['funk', 'hip-hop', 'pop', 'jazz']
let playlistFiltrada = playlist.filter(musica => musica.length > 4)

console.log(`a playlist filtrada (músicas mais de 4 letras): ${playlistFiltrada}`)