let playlist = ['funk', 'hip-hop', 'pop', 'rock', 'jazz', 'reggae', 'classico', 'indie']
console.log(`parte da playlist (indice 2 a 5): ${playlist.slice(2, 6)}`)

console.log(`parte da playlist (indice 0 a 3): ${playlist.slice(0, 4)}`)
console.log(`apenas rock: ${playlist.slice(3, 4)}`)